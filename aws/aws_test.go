package aws

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func Test_WaitForAWSCredsToBeActive(t *testing.T) {
	Convey("WaitForAWSCredsToBeActive()", t, func() {

		Convey("Checks if IAM credentials are ready", func() {
		})

	})
}
